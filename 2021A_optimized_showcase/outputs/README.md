# outputs 目录说明

此目录存放程序自动生成的成果文件，是 GitHub 展示的核心素材。

- `images/`：静态图（仪表板、分析图）
- `gifs/`：动图（封面动效、过程演化）
- `summary.json`：关键数值结果与文件清单

## 使用建议

- 在提交前先运行 `python run_showcase.py`，确保素材与代码版本一致。
- `summary.json` 可作为论文与 README 的指标来源，避免手工抄写出错。
