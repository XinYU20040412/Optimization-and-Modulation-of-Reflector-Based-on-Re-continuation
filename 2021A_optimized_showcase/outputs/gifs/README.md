# gifs 子目录

这里保存动画成果，默认包括：

- `github_cover.gif`：推荐作为 GitHub 封面动图
- `process_sweep.gif`：参数扫描过程动画

## GitHub 展示建议

```markdown
![](2021A_optimized_showcase/outputs/gifs/github_cover.gif)
```

如果希望主页加载更快，可保留 `github_cover.gif`，把 `process_sweep.gif` 放到下方折叠区域。
